NOTE: The steps below are out of date. Please see our readme in the main directory.

Compilation steps:

cd ../..
git clone --recursive https://github.com/libigl/libigl.git
mkdir -p libigl/include/GL
curl https://www.opengl.org/registry/api/GL/glcorearb.h > libigl/include/GL/glcorearb.h
cd cmput414/test
./build.sh

Example usage:
./cmput414_bin cow.obj 6
